# 2026-02-13

## System & Team Configuration
- Workspace was reset earlier; identity/persona restored.
- **User/assistant identity restored**:
  - Assistant: 자비스 (Jarvis)
  - User: 마보스
- **Agents registered in OpenClaw config (actual system registration complete)**:
  - main (Jarvis Main)
  - planner
  - developer
  - researcher
  - writer
- **Model policy changed to OpenAI (not Gemini)** with role-based cost split:
  - planner: `openai-codex/gpt-5.3-codex`
  - developer: `openai-codex/gpt-5.3-codex`
  - researcher: `openai-codex/gpt-5-mini`
  - writer: `openai-codex/gpt-5-mini`

## Telegram / Gateway
- Telegram pairing approved and conversation verified from Telegram user id `8268222214`.
- Gateway config patched multiple times successfully; restart sentinels confirmed.
- OpenClaw gateway auto-start on boot already active via LaunchAgent (`ai.openclaw.gateway.plist`).

## Stock Trading Bot (kis-trading-bot)
### Recovery / Restore
- Google OAuth completed with `client_secret.json` + `token.json`.
- Downloaded backup from Drive: `kis-trading-bot-package.tar.gz`.
- Restored to: `/Users/master/.openclaw/workspace/kis-trading-bot/`.
- Installed dependencies and validated scripts.

### Config / Access
- `kis-demo-config.json` created from sheet values.
- Account number handling fixed to allow plain `50158566` (without `-01` input), internally defaults to product code `01` when omitted.
- Balance check succeeded after config change.

### Code Improvements Applied
- `trading_engine.py` hardened:
  - duplicate order prevention (symbol lock)
  - cooldown per symbol
  - pre-order holding/cash checks
  - post-order position-change confirmation polling
  - runtime state persistence (`runtime-state.json`)
  - date handling fixes
- `kis_api.py` hardened:
  - shared request wrapper with retry/backoff
  - 5xx retry
  - network exception retry
  - 401 token refresh retry path
- Added trading-day guard:
  - weekend skip
  - holiday file support (`market-holidays.json`)
- Generated 2026 KR holiday list into `market-holidays.json` (weekday holidays).
- Fixed dry-run behavior so it does not persist runtime state.

### Scheduling / Rehearsal
- Registered 3 cron jobs (isolated -> Telegram delivery):
  1) 08:30 pre-check
  2) 09:00 auto-start engine
  3) 15:30 EOD report
- Forced-run rehearsal executed:
  - pre-check summary succeeded
  - auto-start succeeded (engine process confirmed)
  - EOD report job executed; reported no fills for test run and fallback summary

## Instagram Automation (next active task)
- User wants to proceed to Instagram auto-upload next.
- Browser relay status checked: Chrome relay up, but no attached tab yet.
- Waiting on user action: open IG tab and click **Attach this tab** in OpenClaw Browser Relay.

## User Preferences
- Strong preference for cost-effective model routing by task complexity.
- Wants practical execution first, then incremental hardening.
