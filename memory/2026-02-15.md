# 2026-02-15

- User preference (locked): do not send chat replies about routine comment-worker runs.
- Applied: both IG comment-worker cron jobs keep running but with `delivery.mode=none` (silent operation).
- User requested autonomous execution without waiting for orders; Jarvis set to continue IG automation project to completion with proactive only milestone/issue reporting.
- Added autonomous schedules:
  - IG Axis1 Pipeline (every 4h)
  - IG Auto Launcher (daytime every 2h)
  both configured silent (`delivery.mode=none`).
- User requested `pipeline_log` be restructured to one-row timeline per post lifecycle.
  - Implemented new timeline columns in `hq-media-app/src/pipeline_sheet.py`:
    생성일시, 계정, media_type, 트랜드 수집 완료, 자동 브리프(캡션/훅/CTA), 프롬프트 생성, 이미지/동영상 생성, 승인 대기(텔레그램 전달), 자동 검증/업로드 예약 확정(무응답 1시간), 최종 상태, 결과값/링크, content_id, schedule_at, note.
  - Image media_type normalized to `carousel`.
  - `comment_worker` excluded from pipeline_log by code.
  - Existing pipeline_log rows migrated to the new timeline header format.
