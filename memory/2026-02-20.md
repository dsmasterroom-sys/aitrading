# 2026-02-20 (KST)

## Cycle2/3 운영 진행 요약
- 사용자가 Pipeline Log 규칙 강화를 지시:
  - 단계별 즉시 업데이트
  - 단계 셀 표기: `대기 / 생성 중 / result`
  - 상단 빈 행부터 채우기
  - brief 컬럼은 생성된 브리프 원문값 기록
- 반영 조치:
  - `hq-media-app/src/pipeline_sheet.py` 수정
    - 상태 라벨 `result`로 통일
    - 빈 행 우선 채움 로직 추가
    - brief 컬럼 원문 기록 우선 처리
  - `sop.md` 문구 동기화

## 시트/사이클 운영
- 사용자 요청으로 pipeline_log 38행 이후 삭제 후 새 단계 시작.
- `CYCLE-20260220-03` Plan 시작 및 5행 기준 진행.
- trend/brief/prompt 단계가 시트에 반영되도록 운영 중.

## 생성 엔진 관련 중요 정정
- 사용자 지적으로 확인된 사실:
  - 현재 hq-media-app은 운영 보조(브리프/프롬프트 파일/로그/PDCA)로 사용 중.
  - 실제 생성은 Flow 웹 경로가 주력.
- 핵심 갭:
  - hq-media-app 실행 엔진의 실제 prompt 경로가 여전히 `src/prompt_composer.py`(구 방식)를 타고 있음.
  - 따라서 `MASTER_PROMP.rtf` + `[GenArchive] UGC_visual_styleguide.rtf`가 생성 시 100% 강제 반영되지 않는 상태.
- 후속 액션 필요:
  - 생성 경로를 템플릿 결합형(마스터+스타일가이드 고정 주입)으로 강제 전환.

## API 상태
- Gemini API 단순 이미지 테스트도 `503 UNAVAILABLE (high demand)` 재현.
- 참조 이미지 업로드는 성공하나 `generating...` 단계에서 지연/중단이 반복.
- 운영 폴백: 웹 Flow 생성 경로 유지.

## Flow 대안/APIYI 검증 메모
- 사용자 공유 링크(apiyi.com 도움말) 검토 결과, "공식 우회"라기보다 **제3자 중계 서비스 소개 성격**으로 판단.
- 실제 엔드포인트(`https://api.apiyi.com/v1/videos/generations`)는 응답하지만, 키 없이 호출 시 `401 Unauthorized` 확인.
- 워크스페이스/환경변수 기준 APIYI 키(`APIYI_API_KEY`)는 미설정 상태였음.
- 결론: APIYI는 근본 해결이 아니라 벤더 전환 옵션이며, 키/비용/정책 종속 리스크 존재.

## Flow 다운로드 블로커(중요)
- 사용자 실제 증상: Flow에서 다운로드 완료로 보여도 로컬 다운로드 폴더에 파일이 없거나, 파일명 확장자 누락 현상.
- 외부 커뮤니티 탐색 결과:
  - Reddit `r/VEO3`에서 **export failures / partial renders** 유사 증상 다수 확인.
  - 동일 스레드의 경험적 워크어라운드로 "**Export one at a time**" 언급 확인.
  - 참고 링크: `https://www.reddit.com/r/VEO3/comments/1qjaaaj/struggling_with_google_flow_consistency_guardrail/`
  - Google Help 커뮤니티에도 Flow 영상 다운로드 문제 스레드 존재:
    - `https://support.google.com/gemini/thread/402759476/google-flow-video-download-problem?hl=en`
- 로컬 1차 점검:
  - `~/Downloads`에서 `.crdownload`/`Unconfirmed` 임시파일 잔존은 당시 발견되지 않음.
