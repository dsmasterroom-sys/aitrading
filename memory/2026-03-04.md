# 2026-03-04

## Durable Notes

- 마보스님이 프롬프트 생성 규칙을 명확히 고정함:
  1) 예시 문장/레퍼런스 문구 verbatim 복붙 금지 (완전 신규 생성)
  2) 출력 라벨은 `Shot 1`, `Shot 2`, `Shot 3` ...만 사용
  3) `Example`, `Sample`, `Plaintext` 같은 제목 금지
  4) 요청 개수 N개 raw prompt만 출력

- @gena_feed 캐러셀 발행 확인 중 429 rate limit 발생. 마보스님 지시로 재시도는 `홀드` 상태로 유지.

- 이미지 생성 품질 관련 핵심 피드백:
  - fallback 텍스트 렌더 결과는 불허
  - 제품 반영/디자인 반영 안 되면 실패로 간주
  - "무조건 나노젠으로 실행" 요구사항 확정

- 운영 규칙 반영:
  - `gena_feed/AGENT.md`에 Designer/Developer 실행 전 Nanogen 서버 자동 확인/자동 시작 절차 추가
  - 자동 시작 절차: 서버 헬스 체크 → OFF면 runserver 실행 → 준비 대기 후 진행

- 실제 Nanogen 실행 방법 확정:
  - 프로젝트 경로: `/Users/master/.openclaw/workspace/nanogen`
  - 이 환경은 시스템 python에 django가 없어 venv python 사용 필요
  - 실행 명령:
    - `cd /Users/master/.openclaw/workspace/nanogen && ./venv/bin/python manage.py runserver 0.0.0.0:8000`
  - 접속 URL: `http://localhost:8000`

- 프로세스 제어 이력:
  - 사용자 요청으로 이미지 생성 전체 강제 종료 수행 (generate_images_nanogen, codex, runserver 포함)
  - 이후 사용자 요청으로 Nanogen 앱 재실행 성공

- W10-01 이미지 생성 시 참조 소스 안내 완료:
  - `weekly/image-prompts.json` (W10-01-reels)
  - `assets/output/product_analysis.json`
  - `assets/outfit/` 최신 참조
  - 실행 스크립트 `scripts/generate_images_nanogen.py`

- 마보스님 지시로 "Claude Code/Codex 실행 운영 규칙"을 최상위 운영지침(`AGENTS.md`)에 반영:
  - PTY 필수
  - 장시간 작업은 background + process(log/poll)
  - orphan(child) 프로세스 종료 점검
  - 게이트웨이 경유 불안정 시 직접 CLI 경로 점검
  - 실패 복구 순서(PTY→재실행→로그→정리→직접 CLI→재시도)
