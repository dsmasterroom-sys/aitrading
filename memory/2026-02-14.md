# 2026-02-14

## Durable updates

- GitHub migration for stock trading bot completed.
  - Repo: `https://github.com/dsmasterroom-sys/aitrading.git`
  - Branch `main` successfully pushed and tracking `origin/main`.
  - Added deployment hygiene: root/project `.gitignore`, `requirements.txt`, `DEPLOY_GITHUB.md`, and `sync_config_from_sheet.py`.

- New PC run issue noted: KIS auth error `403 / EGW00002` during token flow.
  - Likely causes tracked: demo/live endpoint mismatch, stale config sync, key/secret mismatch.
  - Provided fast checklist for validation and recovery.

- Instagram operations ownership requested by user.
  - Accounts:
    - `@genarchive.kr` (brand/sales account for 700 bag inventory)
    - `@gena_feed` (AI persona growth + sales funnel account)
  - Goals:
    - Primary: inventory sales (Coupang/SmartStore links)
    - Secondary: follower growth and monetization potential
  - Content direction: Reels + Carousels, trend-reflective, 3 posts/week per account.

- Instagram browser relay status:
  - Chrome tabs attached and accessible for both target accounts.
  - Awaiting final content strategy agreement before automated posting.

- Meta OAuth troubleshooting (Instagram automation critical blocker):
  - Confirmed `localhost` cannot be added to **앱 도메인** because App Domains require a TLD (error shown: "이름 바로 뒤에 상위 수준 도메인...").
  - Current OAuth failure (`앱 도메인에 포함되어 있지 않은 URL 도메인`) persists when using mismatched Facebook OAuth flow.
  - Decided fix path: configure redirect URI under **Instagram 비즈니스 로그인 설정** first, then run Instagram login flow endpoint (not generic Facebook dialog flow).
  - Next actionable: save a valid redirect URI with TLD (e.g., `https://oauth.pstmn.io/v1/callback`) in Instagram login settings, then re-run OAuth and exchange code for fresh token.

- OAuth blocker resolved (partial):
  - Received OAuth code via `https://oauth.pstmn.io/v1/callback?code=...` and successfully exchanged through Instagram endpoint (`api.instagram.com/oauth/access_token`) using `INSTAGRAM_APP_ID`.
  - Exchanged short-lived -> long-lived token via `graph.instagram.com/access_token` (`expires_in=5184000`).
  - Verified token with `graph.instagram.com/me` (`username=gena_feed`, `id=24182072091491989`).
  - Updated source credential sheet (`apiKey`) values: `INSTAGRAM_ACCESS_TOKEN`, `INSTAGRAM_USER_ID_GENA_FEED`.
  - Important nuance: this IG Login token works for `graph.instagram.com` but still fails on `graph.facebook.com` business endpoints (comments/DM) with OAuth 190 parse-token error.
  - Follow-up completed: Facebook Login OAuth re-issued after correcting App Secret mismatch; Graph token exchange succeeded.
  - Verified success on comment path: `/{ig-business-id}/media` and `/{media-id}/comments` now return 정상 데이터.
  - DM path still blocked with OAuth code 3 (`Application does not have the capability to make this API call`), so DM automation requires additional capability/permission enablement.

- Next-step implementation done:
  - Added API comment responder worker: `projects/instagram-monetization-automation/hq-media-app/scripts_comment_worker.py`.
  - Worker supports sheet-based token loading, duplicate-reply state tracking, dry-run/live modes, and pipeline_log event recording.
  - Upgraded with brand/persona policy: comment-type classification (`buy/product/community`), account-mode templates (`genarchive`/`gena_feed`), and template rotation.
  - Dry-run validation succeeded after upgrade: scanned 12 comments, 10 reply candidates, 2 skipped (`account_mode=gena_feed`).

- Comment automation is now live and scheduled:
  - Live run completed successfully with real replies (`dry_run=False`): scanned 12, acted 10, skipped 2.
  - Cron schedules registered:
    - Daytime every 20 min (`*/20 9-22 * * *`, Asia/Seoul)
    - Night hourly (`0 23,0-8 * * *`, Asia/Seoul)
  - Subsequent scheduled run confirmed stable with no pending new targets (scanned 12, acted 0, skipped 12).

- Strategic alignment update:
  - User emphasized that in the full Instagram monetization automation project, the highest-priority gap is Axis #1 (traffic acquisition content definition).
  - Agreed direction: content generation must be preceded by trend collection and ideation (trend intel -> idea scoring -> content definition -> generation), rather than generating first.

- Axis #1 execution progressed to runnable v1:
  - Added `scripts_axis1_pipeline.py` to score trend backlog and auto-generate production brief JSON.
  - Verified run outputs:
    - scored file `trend-scored-20260214-230019.json`
    - brief file `hq-media-app/templates/brief.auto.20260214-230019.json`
    - top topic `출근가방 고르는 기준` with score 80.

- Team-process feedback incorporated:
  - User explicitly requested stronger autonomous execution and better visibility of whether Jarvis is working/idle.
  - Implemented lightweight presence app:
    - `projects/instagram-monetization-automation/agent_presence_app.py`
    - `projects/instagram-monetization-automation/set_agent_presence.py`
    - local status URL `http://127.0.0.1:8799`.
  - Current operating rule reinforced: proactive milestone updates without waiting for user prompts.

- Communication preference locked in:
  - User requested no chat replies for comment-worker routine runs (acted=0 style updates should be silent).
  - Cron delivery for both comment-worker jobs changed to `delivery.mode=none` while keeping jobs enabled (automation continues without chat notifications).
